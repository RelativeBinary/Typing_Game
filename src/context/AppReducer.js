// Manages application state changes in response to certain action to the context
export default (state, action) => {
  switch (action.type) {
    //change difficulty case

    //start new game case
    default:
      return state;
  }
};
